<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.unipiloto.appvacov">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppVacov">
        <activity android:name=".PantallaEntidad"></activity>
        <activity android:name=".PantallaPaciente" />
        <activity android:name=".PantallaPersonal" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroEPS" />
        <activity android:name=".RegistroPaciente" />
        <activity android:name=".TipoUsuario" />
        <activity android:name=".RegistroPersonal" />
        <activity android:name=".popup_vacuna"/>
    </application>

</manifest>